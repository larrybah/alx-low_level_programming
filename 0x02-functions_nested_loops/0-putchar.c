#include "main.h"

/**
 * main - prints text to the stdout
 * Return: 0 on Success.
 */
int main(void)
{
	char s[] = "_putchar";
	int i;

	for (i = 0; i <= s[i-1]; i++)
	{
		_putchar(s[i]);
	}
	return (0);
}
