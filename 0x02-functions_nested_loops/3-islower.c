#include "main.h"
#include <ctype.h>
/**
 * main - A program that checks if character is lower
 * Return: 0 (Success)
 */
int _islower(int c)
{
char = c;
if (islower(c))
{
	_putchar('1');
}
else 
{
	_putchar('0');
}
}
