#include "main.h"
#include <ctype.h>
/**
 * _islower - A program that checks if character is lower
 * @c check if 'c' is lower
 * Return: 0 (Success)
 */
int _islower(int c)
{
char = c;
if (islower('c'))
{
_putchar('1');
}
else
{
_putchar('0');
}
}
