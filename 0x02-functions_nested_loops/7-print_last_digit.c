#include "main.h"

/**
 * print_last_digit - prints the last digit of a value
 * @r: value to be return
 * Return: value
 */
int print_last_digit(int i)
{
	if (i % 10 == 0)
	{
		return (i);
	}
	return (i);
}
