#include "main.h"
/**
 * _isdigit - checks if a character is a number
 * @c: number to be returned
 * Return: number
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}
