#include "main.h"
/**
 * _isdigit - prints digit from 1 throught 9
 * @c: parameter
 * Return: 1 (Success)
 */
int _isdigit(int c)
{
	while (c >= 0 && c <= 9)
	{
		if (c == true)
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
}
