#include "main.h"
/**
 * _isdigit - prints digit from 0 throught 9
 * @c: parameter
 * Return: 1 (Success)
 */
int _isdigit(int c)
{
int c;
if (c >= 0 && c < 10)
{
return (1);
}
else
{
return (0);
}
}
