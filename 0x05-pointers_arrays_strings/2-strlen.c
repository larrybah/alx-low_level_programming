#include "main.h"
/**
 * _strlen - prints the lenght of a string
 * @s: parameter
 * Return: length
 */
int _strlen(char *s)
{
	int length;

	while (*s != '\0')
	{
		length++;
		c += 1;
	}
	return (length);
}
