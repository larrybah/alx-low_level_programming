#include "main.h"
/**
 * print_rev - print string in reverse
 * @s: variable
 */
void print_rev(char *s)
{
	int i, j;

	i = i + j;
	j = i - j;
	j = i - j

	for (i = s - 1; i < j; i--)
	{
		if (i == '\0')
		{
			_putchar('i');
		}
	}
	-putchar('\n');
}
