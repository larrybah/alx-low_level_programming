#include "main.h"
/**
 * *_strcat - concatinate two strings adn prints the result
 * @dest: Destination
 * @src: Source
 * Return: 0 (Success)
 */
char *_strcat(char *dest, char *src)
{
	char *dest[100];
	char *src[];
	char strcat[];

		if (*dest == '\0')
		{
			strcat = *dest + *src;
			_putchar(strcat);
		}
	_putchar('\n');
}
