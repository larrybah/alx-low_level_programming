#include "main.h"
/**
 * leet - print characters in leet format.
 * Return: leet characters.
 */
char *leet(char *str)
{
	int i;
	int *ptr;
	
	ptr = str[0];

	for (i = 0; i < str[i - 1]; i++)
	{
		
	}
}
