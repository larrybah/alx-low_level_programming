#include "dog.h"
/**
 * print_dog - prints the elements of dog
 */
void print_dog(struct dog *d)
{
	struct dog my_dog;

	if (*d == NULL)
	{
		return;
	}

	if (d->mydog == NULL)
	{
		printf("nil\n");
	}
	else if (my_dog.name = NULL)
	{
		printf("Name: (nil)\n");
	}
}
